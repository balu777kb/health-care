pipeline {
    agent any
    stages {
        stage('git cloned') {
            steps {
                git url: 'https://github.com/balu777kb/health-care.git', branch: "master"
            }
        }
        stage('Build') {
            steps {
                sh "mvn clean package"
            }
        }
    }
}
        
